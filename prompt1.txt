You are converting a standard non-interactive competitive programming problem into an implementation-ready intermediate representation.

Your task is to describe exactly one concrete correct algorithm that matches:
- the exact problem statement,
- all formal definitions and operators,
- the exact input/output format,
- and the time and space complexity implied by the constraints.

The IR will be given to a separate code-generation model that will receive nothing else.
Therefore the IR must be a closed implementation contract: the code generator must not need to guess any parsing detail, grouping boundary, indexing convention, equality case, maintained-state meaning, update rule, or output convention.

Requirements:

1. Preserve the original semantics exactly. Do not add assumptions, simplify the task, reinterpret the statement, or replace the required algorithmic object with an equivalent-looking but differently defined one.
2. Describe exactly one concrete algorithm.
3. Preserve every exact operator and condition, including strict vs. non-strict inequalities, equality cases, inclusive vs. exclusive endpoints, modulo rules, bitwise rules, lexicographic rules, tie-breaking rules, and all quantified ranges.
4. State every implementation-critical rule explicitly. Nothing important may be left implicit.
5. For every maintained variable or data structure, state what it stores, what each stored item means, and how it changes.
6. The program first reads and parses the complete required input from standard input before algorithmic processing begins. The IR must first define the already-parsed input objects and then describe the algorithm operating on those objects. Do not describe online reading, streaming updates during input consumption, or interleaving input parsing with algorithmic state updates, unless the original statement explicitly defines an interactive or online-input task. This task is non-interactive.
7. Every input quantity must be classified by its physical parsing unit: scalar token, fixed-length token block, repeated tuple block, or remaining stream block. The IR must preserve that physical parsing unit exactly. Do not rewrite a block of multiple tokens as repeated scalar reads, and do not rewrite repeated scalar reads as one aggregated block, unless the original statement explicitly does so.
8. Parsing-unit preservation must be local, not merely global. If the official input gives one line containing n integers, the IR must explicitly say that these n integers are parsed together as one fixed-length token block representing one whole object. If the official input gives n subsequent lines each containing one integer, the IR must explicitly say that they form a repeated tuple block of length-1 tuples. State the exact layout that the parser consumes, not a logically equivalent alternative.
9. All problem-defined position-like quantities — including indices, coordinates, endpoints, ranks, labels, node numbers, permutation values, query ids, and any output computed from them — must remain in the original convention of the problem statement throughout the IR. Do not renumber them to 0-based indexing or any other convenience convention anywhere in the IR, even temporarily.
10. Any maintained state that stores, refers to, compares, removes, inserts, or outputs a position-like quantity must explicitly state that quantity in the original problem convention. It is not enough to say only that the final output is converted back; the maintained state itself must already use the original convention.
11. Any quantity that contributes to the final output must explicitly state its semantic convention. If it depends on positions or labels, the IR must say exactly which convention those positions or labels use.
12. The stated time and space complexity must match the described algorithm.
13. Do not include code or pseudocode.
14. Do not include editorial discussion, motivation, alternatives, heuristics, or remarks about how easy the task is.
15. Do not rely on “obvious implementation details”, “standard parsing”, “usual indexing”, “language-native arrays”, or similar shortcuts. Replace all such shortcuts with explicit statements.
16. Redundantly restate the two highest-risk conventions — exact parsing units and exact position/index convention — in every section where they matter: [1], [2], [4], [6], [7], and [8].
17. Whenever the official statement uses a position convention such as 1-based indexing, [4] must name maintained items using that same convention, and [6] must describe updates using that same convention. Do not describe updates using Python-style offsets.
18. Whenever the official statement uses a structured input block, [6] must refer to the already-parsed block by name. Do not describe [6] as if values are being fetched from stdin one by one.
19. If the required algorithm processes test cases, prefixes, suffixes, queries, operations, or events in a certain order, describe that order over the already-parsed objects. Do not blur parsing order and processing order.
20. In [8], explicitly state which tempting misreadings would be wrong for this problem whenever relevant, especially: changing a fixed-length block into repeated scalar reads, changing repeated scalar reads into one aggregated block, and changing the original position convention into 0-based or language-native indexing.
21. If the official statement defines multiple cases or grouped records, [1] must assign a distinct object name to each parsed layer, and [6] must refer to those same object names rather than inventing a different parser narrative.
22. If a quantity is derived from another quantity whose convention matters, explicitly state the convention of both the source and the derived quantity.
23. If the algorithm maintains an incremental answer or aggregate, explicitly state whether it is updated after each processed object or computed only after all processing is finished.
24. You must include in your explanation that if the main function is defined, then at the end of the code, it needs to be called to ensure that the entire code can run and produce output.

Formatting rules:

1. Output only raw Markdown and LaTeX source.
2. The first non-empty line must be exactly:
Implementation Specification
3. After that, output exactly the eight section headers below, exactly as written.
4. Each section header must begin with `###`.
5. Do not output any other heading or section.
6. Do not use fenced code blocks.
7. End the output immediately after section [8].
8. Inside the eight required sections, use complete sentences. Do not output placeholders such as “same as statement” or “standard”.

Output format:

Implementation Specification

### [1] Problem Interface
State the exact program-level input/output interface.
For each part of the input, specify all of the following:
- its meaning,
- its physical parsing unit,
- its exact layout position in the official input,
- whether it is read alone, as part of a fixed-length token block, as one tuple of a repeated tuple block, or as a remaining stream block,
- and the exact already-parsed object name that will be used later in the algorithm description.
Also specify:
- the exact input layout, including lines, token groups, counts, ordering, and grouping,
- what logical object each parsed part represents,
- what the program writes to standard output,
- and the exact output layout.
The parser implied by the IR must consume exactly the same grouping as the official input format.
This section must also explicitly identify any parsing alternatives that would be wrong if they changed the official grouping.

### [2] Formal Definitions
State the exact mathematical or logical definitions that determine correctness.
Define the input variables, derived variables, and output variables, and the exact condition, objective, or relation that the output must satisfy.
If the statement gives a formal definition of a valid object, transition, segment, set, score, cost, answer, or query result, reproduce it explicitly.
All position-like quantities in the formal definitions must use the original convention of the problem statement.
If an answer is defined from positions, indices, coordinates, labels, or ranks, explicitly state that convention again here.

### [3] Required Complexity
List the relevant constraints and state the required asymptotic time and space complexity for the one algorithm being specified.
The complexity must be strong enough to rule out slower recomputation-based interpretations when the constraints require an incremental or amortized method.

### [4] Maintained State
List every maintained variable and data structure.
For each one, state:
- its name,
- its type or structure,
- its contents,
- what each stored item means,
- and what it represents at any moment.
If it stores values associated with positions, indices, coordinates, labels, ranges, endpoints, or ranks, explicitly state that those stored quantities use the original problem convention, not a language-native offset.
If a data structure stores pairs or tuples, specify the meaning and convention of every component.

### [5] Invariants
List the invariants that hold after initialization and after every processing step.
Each invariant must be precise enough to justify the update rules.
Whenever an invariant mentions stored positions, ranges, endpoints, labels, or ranks, state them in the original problem convention.
Whenever an invariant depends on already-parsed input objects, refer to those objects by the names introduced in [1].

### [6] Per-Operation Update Rules
Describe the algorithm in exact processing order after the full input has already been read and parsed.
Begin this section from the already-parsed objects named in [1]; do not describe any additional stdin reads here.
For every change, specify:
- the exact object currently being processed,
- the exact condition or comparison used,
- what is removed, added, modified, or queried,
- how each maintained variable or data structure changes,
- and how any incrementally maintained answer changes.
If a step uses a quantity originating from the input, refer to the already-parsed object and preserve the parsing unit declared in [1]. Do not reinterpret a fixed-length token block as repeated scalar reads, and do not reinterpret repeated scalar reads as one block.
If a step updates or stores a position-like quantity, state it in the original problem convention.
If the algorithm scans objects in a certain direction or order, state that order explicitly.
If equality handling matters, state the exact comparison symbol used.

### [7] Output Rule
State exactly how the required output stream is obtained from the maintained state after processing.
If one answer is produced per test case, per query, per operation, per prefix, or per input object, state the exact production order and formatting.
If the output depends on positions, indices, coordinates, endpoints, labels, or ranks, explicitly state that those reported quantities are already in the original problem convention and require no later re-indexing.
If the output is accumulated during processing but printed later, say so explicitly.

### [8] Edge Cases and Consistency Checks
State the boundary and special cases that affect correctness.
Include all relevant equality cases, empty-structure behavior, first-step behavior, last-step behavior, single-element cases, minimum-size cases, maximum-size implications, parsing-unit consistency, and indexing or labeling conventions.
This section must explicitly verify consistency between [1], [2], [4], [5], [6], and [7].
Also explicitly state the relevant wrong-but-tempting interpretations that must not occur for this problem, if any such interpretation is plausible from common competitive-programming habits. In particular, mention wrong parsing regroupings and wrong 0-based or language-native reinterpretations whenever relevant.

Output only the specification in this format.