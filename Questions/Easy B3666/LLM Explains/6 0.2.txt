## Algorithm Description

### [1] Problem Summary

The problem requires finding the suffix maximum values' indices in a dynamically growing sequence and calculating their bitwise XOR after each insertion.

### [2] Formal Problem Definition

Given a sequence $a$ that is initially empty, we need to process $n$ insertions of positive integers $x$. After each insertion, we must find all suffix maximum values' indices in the current sequence $a$ and calculate their bitwise XOR.

### [3] Input Specification

- The first line contains an integer $n$, representing the number of insertions.
- The second line contains $n$ integers $x_i$, representing the values to be inserted.

### [4] Output Specification

After each insertion, output the bitwise XOR of the indices of all suffix maximum values in the current sequence.

### [5] Constraints

- $1 \leq n \leq 10^6$
- $1 \leq x_i < 2^{64}$

### [6] Key Observations

- The problem can be solved by maintaining a data structure that allows efficient insertion and querying of suffix maximum values.
- The bitwise XOR operation can be used to combine the indices of suffix maximum values.

### [7] Algorithm Idea

- Use a stack or a similar data structure to keep track of the indices of the suffix maximum values.
- When a new element is inserted, update the stack accordingly to maintain the correct suffix maximum values.
- Calculate the bitwise XOR of the indices of the suffix maximum values after each insertion.

### [8] Detailed Algorithm Steps

1. Initialize an empty stack to store the indices of suffix maximum values.
2. For each insertion:
   - While the stack is not empty and the top element is smaller than the current element, pop the stack.
   - Push the current index onto the stack.
   - Calculate the bitwise XOR of the indices in the stack.
3. Output the bitwise XOR result after each insertion.

## Implementation Details

- The algorithm uses a stack to efficiently manage the suffix maximum values.
- The stack is updated dynamically with each insertion.
- The bitwise XOR operation is used to combine the indices of suffix maximum values.

## Example Walkthrough

Given the input sequence $[2, 1, 3, 5, 4]$, the algorithm processes each insertion as follows:

- Insertion 1: $2$ - Stack: $[0]$ - XOR: $0$
- Insertion 2: $1$ - Stack: $[0, 1]$ - XOR: $0 \oplus 1 = 1$
- Insertion 3: $3$ - Stack: $[0, 1, 2]$ - XOR: $0 \oplus 1 \oplus 2 = 3$
- Insertion 4: $5$ - Stack: $[0, 1, 2, 3]$ - XOR: $0 \oplus 1 \oplus 2 \oplus 3 = 3$
- Insertion 5: $4$ - Stack: $[0, 1, 2, 4]$ - XOR: $0 \oplus 1 \oplus 2 \oplus 4 = 7$

The output after each insertion is $1, 3, 3, 4, 1$.